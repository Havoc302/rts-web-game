RTS Web Game - AI Work Log

Agent handoff protocol:
- This file is the resumable implementation log used by different AI agents.
- At the start of a generation, add or update a `Current task` block with the intended work and checklist.
- At the end of a generation, mark completed items `[done]`, record validation, and leave unfinished items unchecked with enough context for another agent to continue.
- Do not erase historical entries. The current task at the bottom is the active handoff; `docs/DESIGN.md` is the architecture and product source of truth.

Current task: Add standalone JSON game save export/import.

- [done] Serialize the complete paused game state into a versioned JSON document.
- [done] Add Save/Export and Load/Import controls to the game UI.
- [done] Restore every tile, producer, simulation setting, stockpile, treasury, camera, and UI state; imported games always return paused.
- [done] Add paused-only validation and round-trip regression tests.

Current task: Require tunnel access for enclosed mountain surveys.

- [done] Block surveying a mountain tile only when all four orthogonal neighbors are mountains without tunnels.
- [done] Allow surveying an enclosed mountain once an adjacent tunnel provides access; exposed mountain tiles remain surveyable.
- [done] Correct road/tunnel coverage-version invalidation while updating the shared Grid placement path.

Current task: Record military production economics for the Command & Conquer phase.

- [done] Document that non-consumer-goods production on industrial zones suspends that tile's tax income for the production duration.
- [done] Document the treasury expense for requisitioning private industry to produce military goods.
- [done] Document dedicated military construction buildings and their 2x equivalent industrial production rate.

Validation:
- `docs/DESIGN.md` updated to rev 7 with the new Phase 2 rule and Key Decision 14.
- No runtime behavior changed; implementation remains outstanding for the future war-economy phase.

Current task: Add hidden ores and a utility-powered survey building.

- [done] Add simplified ore types and per-tile hidden ore generation, with much higher mountain odds.
- [done] Add Survey Building configuration, placement, utility requirements, cost, and toolbar control.
- [done] Add one-square-at-a-time surveying with 10-tick standard duration and 20-tick mountain duration.
- [done] Add survey progress/results to rendering and tile inspection.
- [done] Add focused tests for ore generation, survey timing, utility shortfall, and discovery.
- [done] Run validation and record final status.

Validation:
- `npm run test:ore` passes.
- Syntax and editor diagnostics pass for modified source files.
- `npm test` reaches the existing pollution-growth assertion failure in Test 9; the ore/survey tests pass independently.

Current task: Simplify the discovered-ore map marker.

- [done] Replace the centered discovered-ore circle with a tiny red corner X.

Validation:
- Renderer syntax and diagnostics pass.

Current task: Limit and attenuate sewage pollution traveling downstream in rivers.

- [done] Make river pollution strength depend on sewage discharge volume and fall off by 10 per tile.

Validation:
- `npm run test:river` passes.
- Pollution and renderer syntax and diagnostics pass.

Current task: Clarify Survey Station road and utility warnings.

- [done] Ensure a road-connected Survey Station does not report a misleading road error when utilities are missing.

Validation:
- `npm run test:ore` passes.
- Main and renderer syntax and diagnostics pass.

Current task: Combine river pollution from adjacent sewage plants.

- [done] Accumulate sewage discharge contributions when multiple plants feed the same river path.

Validation:
- `npm run test:river` passes, including the two-plant regression case.
- Pollution implementation and test syntax and diagnostics pass.

Current task: Correct Survey Station utility status in the Tile Inspector.

- [done] Show actual Survey Station utility connectivity instead of zoned-tile road-network messages.

Validation:
- `npm run test:ore` passes.
- Main syntax and diagnostics pass.

Current task: Reduce starting treasury and clarify percentage-based tax income.

- [done] Lower starting money from $10,000 and make the income display explicitly reflect the selected tax percentage.

Validation:
- Tax percentage test passes, including the existing 150% calculation case.
- Config, main, and HTML diagnostics pass.
- Full suite reaches the existing downstream river-contamination assertion failure.

Current task: Fix river-pollution browser hang and rename Water Tower to Water Pump.

- [done] Prevent repeated river-loop processing and update all user-facing water tower labels.

Validation:
- `npm run test:river` passes.
- `npm run test:ore` passes.
- Full suite no longer hangs but reaches the existing downstream water-tower assertion in Test 8.
- Pollution/config/HTML diagnostics pass.

Current task: Stop idle sewage plants from polluting rivers.

- [done] Base river discharge on actual sewage throughput so zero production creates zero pollution.

Validation:
- `npm run test:river` passes, including the idle-plant assertion.
- Pollution implementation and regression test syntax and diagnostics pass.
- Full suite Test 8 still expects idle sewage plants to pollute at capacity and therefore fails by design under the corrected behavior.

Current task: Restrict 67-unit sewage pollution to the intended downstream range.

- [done] Prevent sideways/reverse river traversal while retaining combined discharge and 10-per-tile falloff.

Validation:
- Generated-map reproduction no longer expands across the whole river or overflows strength values.
- `npm run test:river` passes.
- Pollution syntax and diagnostics pass.

Current task: Cap residential growth score at full population.

- [done] Stop growth score at the score where a high-density residential tile reaches its population capacity.

Validation:
- Growth score is capped at 50, matching full high-density residential population.
- `npm test` passes all 18 verification tests.
- Growth configuration, simulation, and regression test diagnostics pass.

Current task: Show active Survey Station power demand in the HUD.

- [done] Include the Survey Station's active survey power draw in `powerDemand` while a survey is running.

Validation:
- `npm run test:ore` passes with idle and active HUD demand checks.
- Simulation and regression test syntax and diagnostics pass.

Current task: Stabilize zone densities and add service operating budgets.

- [done] Keep residential, commercial, and industrial density upgrades permanent.
- [done] Add Police, Fire, Hospital, School, and Library running costs with adjustable budgets affecting jobs and coverage.

Validation:
- `npm run test:services` passes, including reduced-budget jobs, coverage, and cost checks.
- `npm test` passes all 18 verification tests.
- Density, service-budget, treasury, and UI diagnostics pass.

Current task: Make Police Station size, staffing, cost, and coverage population-driven.

- [done] Add police officer caps by density, population-based staffing, per-officer construction cost, and officer-based coverage.

Validation:
- `npm run test:services` passes, including the two-officer minimum and one-officer-per-thousand scaling.
- `npm test` passes all 18 verification tests.
- Police configuration, service manager, placement, UI, and test diagnostics pass.

Current task: Restore fixed civic construction costs and extend population staffing to Fire and Hospital.

- [done] Keep Police, Fire, and Hospital build prices fixed while sharing population-based staffing caps and officer-based coverage.

Validation:
- `npm run test:services` passes with fixed Police, Fire, and Hospital construction costs.
- `npm test` passes all 18 verification tests.
- Civic service configuration, placement, manager, and test diagnostics pass.

Current task: Add road-tile maintenance costs.

- [done] Charge $1 per road or bridge tile per simulation tick and show the expense in the HUD.

Validation:
- `npm run test:roads` passes for roads and bridges.
- `npm test` passes all 18 verification tests.
- Road maintenance accounting and HUD diagnostics pass.

Current task: Make income per tick population and occupancy based.

- [done] Replace fixed density income with actual residential population and filled commercial/industrial job occupancy.

Validation:
- `npm run test:income` passes.
- `npm test` passes all 18 verification tests.
- Population-based income and tax regression diagnostics pass.

Current task: Start the simulation paused.

- [done] Initialize the game with the pause control active and no simulation interval running.

Validation:
- Startup pause syntax and HTML diagnostics pass.

Current task: Make industrial zones cost $30 and pay more income.

- [done] Apply the industrial placement cost and higher income rates to offset pollution.

Validation:
- `npm run test:industrial` passes.
- `npm run test:income` passes.
- `npm test` passes all 18 verification tests.
- Industrial cost, income, placement UI, and diagnostics pass.

Current task: Tax residents and employed workers directly.

- [done] Calculate tax revenue as $1 per 100 residents plus $1 per 100 employed workers per tick, scaled by the tax rate.

Validation:
- `npm run test:tax-revenue` passes.
- `npm run test:income` passes.
- `npm test` passes all 18 verification tests.
- Tax revenue configuration, simulation, and regression diagnostics pass.

Current task: Reduce school operating cost to match actual staffed jobs.

- [done] Stop school costs multiplying against total city population and base them on staffed school jobs instead.

Validation:
- `npm run test:school` passes.
- `npm run test:services` passes.
- `npm test` passes all 18 verification tests.
- School cost, service accounting, and diagnostics pass.

Current task: Scale all monetary values by 10.

- [done] Multiply starting treasury, income, build prices, running costs, road maintenance, and tax revenue by 10 without changing non-money simulation quantities.

Validation:
- `npm run test:tax-revenue` passes.
- `npm run test:roads` passes.
- `npm run test:school` passes.
- `npm test` passes all 18 verification tests.
- Monetary configuration, UI labels, and diagnostics pass.

Current task: Correct excessive tiny-city service expenses.

- [in progress] Base every service running cost on actual staffed jobs instead of multiplying fixed costs by total population.

Current task: Move Bulldoze into the General tools group.

- [done] Reposition the Bulldoze button under General without changing its behavior.

Validation:
- HTML diagnostics pass and the Bulldoze button appears once under General.

Current task: Connect civic services to utilities and scale service buildings with population.

- [done] Add utility status/allocation for Police, Fire, and Hospital buildings.
- [done] Make their coverage and artwork respond to population density tiers.

Validation:
- `npm run test:services` passes.
- Civic service implementation and test syntax and diagnostics pass.

Current task: Add active-survey power consumption.

- [done] Make Survey Stations draw an additional power chunk while surveying a tile.

Validation:
- `npm run test:ore` passes.
- Survey power configuration, allocation, and test syntax and diagnostics pass.

Current task: Redesign taxation as a realistic 0–100% rate.

- [done] Add high-tax pressure to residential population and commercial/industrial job capacity across the 0–100% rate.

Updated behavior: growth slows continuously toward 50%, turns negative at 50%, and reaches strong outflow pressure at 100%.

Validation:
- `npm run test:tax` passes.
- Tax configuration, simulation, and regression test syntax and diagnostics pass.
- `npm test` passes all 18 verification tests.

Current task: Rename the tax slider label.

- [done] Change the visible label from Income Collected to Tax Rate.

Validation:
- HTML diagnostics pass and the Tax Rate label is present.

Current task: Bound industrial sewage river spread.

- [done] Prevent branch/cycle amplification so industrial sewage follows the finite discharge falloff instead of contaminating the whole river.

Validation:
- `npm run test:river` passes.
- Pollution syntax and diagnostics pass.

Current task: Halve residential population capacity to reduce excessive high-density population.

- [done] Halve RESIDENTIAL_CAPACITY for light/medium/high density (50/250/1000 -> 25/125/500).

Validation:
- `npm test` initially failed 3 stale assertions (simulation.test.js Test 5 tax income, Test 11 labor aggregates, Test 14 employment bonus) and tax-revenue.test.js, all tied to the old capacity numbers; expectations were recalculated and updated to match the halved capacity.
- All suites pass (see below).

Current task: Add crime & policing simulation (spawns from unemployment, police suppression, growth penalty, tax revenue loss).

- [done] Add `CRIME_CONFIG` to config.js and `crime: 0` to Grid.js default tile.
- [done] Add `src/engine/CrimeManager.js`: decay, unemployment-driven spawn probability (clamped to [0,1]), police suppression, +2 crime per hit (capped 10), diffusion to unpoliced neighbors above threshold 4.
- [done] Wire `CrimeManager.updateCrime()` into `Simulation.tick()` right after `ServiceManager.updateServices(...)`.
- [done] Apply -2 growth penalty in `updateGrowthAndDensity()` for all zoned tiles at/above `CRIME_PENALTY_THRESHOLD`.
- [done] Deduct per-tile crime tax loss (capped at `MAX_TAX_LOSS_RATIO`) from `stats.incomePerTick` in `computeStats()`.
- [done] Add crime level + tax penalty % to the Tile Inspector (main.js) and a red "🚨 Crime" overlay mode (Renderer.js + index.html button).
- [done] Added a deterministic Math.random/CRIME_CONFIG stub in simulation.test.js's growth-threshold test so probabilistic crime doesn't make it flaky.

Validation:
- `npm test` passes all 18 verification tests.
- CrimeManager, Simulation, Renderer, main.js, index.html, config.js, Grid.js diagnostics pass.

Current task: Fix stale 10x-monetary-scaling test expectations (from an earlier "Scale all monetary values by 10" task that never updated two test files).

- [done] `tests/industrial-economics.test.js`: COSTS.INDUSTRIAL_ZONE assertion updated from $30 to $300.
- [done] `tests/service-buildings.test.js`: Police/Fire/Hospital cost assertions updated from $400/$400/$600 to $4000/$4000/$6000.

Validation:
- All test suites pass: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Route all monetary config values through the single MONEY_MULTIPLIER variable (previously hardcoded as pre-multiplied literals).

- [done] Rewrote STARTING_TREASURY, SERVICE_CONFIG (costs, runningCost, runningCostPerJob), PRODUCER_CONFIG costs, COSTS, ROAD_MAINTENANCE_COST, TAX_REVENUE_CONFIG.MONEY_PER_TAX_UNIT, and BASE_INCOME in config.js as `base * MONEY_MULTIPLIER` instead of pre-scaled literals. Resulting values are unchanged (base values are the pre-10x numbers), so future scale changes only require editing MONEY_MULTIPLIER.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Expand default map size from 150x150 to 200x200.

- [done] Set MAP_WIDTH/MAP_HEIGHT to 200 in config.js.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Close the Tile Inspector panel when a non-Inspect tool is selected.

- [done] Hide `#inspector-panel` by default (CSS `display:none`, shown via a `.visible` class) and only add `.visible` when a tile is clicked with the Inspect tool active.
- [done] Clear `renderer.selectedTile` and remove `.visible` whenever any other tool button is clicked.

Validation:
- main.js and style.css diagnostics pass. No automated test covers this UI-only behavior.

Current task: Make scroll-wheel zoom center on the mouse cursor instead of the map center.

- [done] In the canvas `wheel` handler, compute the world point under the cursor before zooming, then solve for the new camera offset so that same world point stays under the cursor after the zoom factor is applied.

Validation:
- main.js diagnostics pass. No automated test covers this UI-only behavior.

Current task: Increase forest air pollution absorption to a more realistic level.

- [done] Raised FOREST_POLLUTION_ABSORPTION from 2 to 6 (per-tile pollution values top out around 15 from a contaminated water tower and 10 from high-density industrial, so 2 was only ~13-20% mitigation; 6 brings a forest tile closer to real-world urban-canopy particulate reduction estimates of 15-40%).

Validation:
- No test references FOREST_POLLUTION_ABSORPTION directly. All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Add Windmill, Solar Panel, Battery Storage, Coal Power Plant, and Nuclear Power Plant, plus a day/night cycle.

- [done] config.js: added PRODUCER_TYPE entries + POWER_PRODUCER_TYPES list, day/night constants (TICKS_PER_HOUR=1, HOURS_PER_DAY=24, DAY_START_HOUR=6, NIGHT_START_HOUR=18, NIGHT_TINT_ALPHA), WIND_CONFIG, SOLAR_CONFIG, BATTERY_CONFIG (400 max storage / 40 discharge per tick, matching the user's 10-tick example), COAL_CONFIG (radius 6 / emission 12), and PRODUCER_CONFIG entries for all five (Nuclear requires water-adjacent for cooling, is the most expensive and highest-capacity, and emits no pollution; Coal is mid-cost but a heavy polluter).
- [done] Grid.js: new Battery producers get `maxStorage`/`storedEnergy` fields; their per-tick `capacity` starts at 0 and is computed dynamically.
- [done] RoadNetwork.computeProducerDistances and UtilityManager.allocateUtility now accept an array of producer types (backward compatible with a single type string), so all power-generating buildings share one allocation pool.
- [done] UtilityManager: added `updatePowerGeneration` (windmill = base capacity +/- random fluctuation each tick; solar = sine bell curve between DAY_START_HOUR and NIGHT_START_HOUR, 0 at night), `settleBatteries` (depletes stored energy by whatever was discharged that tick), and `chargeBatteries` (recharges from OTHER producers' unused generated capacity, i.e. supply minus what tiles actually drew).
- [done] Simulation: `getHourOfDay()`/`isDaytime()` derived from tickCount; `tick()` now passes the current hour into `UtilityManager.allocateAll`; `computeStats()` power-capacity aggregation now recognizes all POWER_PRODUCER_TYPES instead of only 'power_plant'.
- [done] PollutionManager: added a coal-plant smokestack emission source (same radius/falloff model as industrial zones).
- [done] Renderer: added a flat semi-transparent night-time tint over the visible map when `!simulation.isDaytime()`, plus distinct tile art for windmill (spinning blades), solar panel, battery (fill-level indicator), coal plant (smokestack), and nuclear plant (cooling towers).
- [done] index.html: added toolbar buttons for all five new producers under Utility Producers, and a HUD clock (`stat-time`) showing sun/moon icon + 12-hour time.
- [done] main.js: producer inspector panel shows battery stored/max energy; render loop and manual `render()` calls now pass `simulation` through for the night tint; fixed the power-producer lookup in the utility status inspector text to recognize all power types instead of only 'power_plant'.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- Manual node scripts confirmed: solar capacity is 0 at night and follows a sine bell curve peaking at noon during the day; windmill capacity fluctuates tick to tick; battery storedEnergy climbs toward maxStorage from surplus generation; coal plant pollution falls off correctly from source (emission) to 0 at radius+1.

Current task: Halve the simulation tick rate (user feedback: too fast).

- [done] Changed the sim interval from `1000 / speed` to `2000 / speed` in main.js so every speed setting (pause/1x/2x/5x) now ticks half as often, without changing the button labels or speed multiplier semantics.

Validation:
- main.js diagnostics pass. No automated test covers this UI timing behavior.

Current task: Stop the simulation from silently advancing (tickCount, crime, growth, surveys) when placing tiles while paused.

- [done] Simulation.tick(advanceWorld = true) now gates tickCount increment, CrimeManager.updateCrime, updateSurveys, and updateGrowthAndDensity behind `advanceWorld`, while still always recomputing utility allocation/pollution/services/stats so the HUD reflects the new placement immediately.
- [done] main.js build-tool success handler now calls `this.simulation.tick(!this.simulation.isPaused)` so placing roads/zones/producers while paused only refreshes stats/treasury instead of advancing the game clock and world state.

Validation:
- All test suites pass unchanged (existing `sim.tick()` calls use the default `advanceWorld = true`): `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Fix crime being driven by the wrong "unemployment" metric, which masked the tax rate's effect on growth.

- [done] Root cause: CrimeManager used `1 - stats.employmentRate` (jobsFilled / totalJobsProvided) as its unemployment signal. That's a job-vacancy-fill ratio, not resident unemployment — in a city with abundant jobs relative to population it reads as near-zero (e.g. 0.01), which the crime scaler (2.5x) blew up into near-guaranteed crime every tick regardless of tax rate, capping tile crime at 10 and permanently zeroing growthScore via the crime growth penalty. That made it look like tax rate no longer affected growth (both 0% and 100% tax stalled at growthScore 0), while employment rate still visibly moved with tax since jobsMultiplier is computed independently.
- [done] Renamed `CRIME_CONFIG.UNEMPLOYMENT_CRIME_SCALER` to `JOB_SCARCITY_CRIME_SCALER` and changed CrimeManager to drive crime probability off resident job scarcity: `(totalEmployablePopulation - jobsFilled) / totalEmployablePopulation` (residents who want work but can't get any, because there aren't enough jobs), instead of the jobs-side employment rate. A city with more jobs than residents now reads as zero scarcity (low baseline crime only) even before every job slot is filled.
- [done] Updated the one test that toggled this constant to the new name.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- Manual node script confirmed: with abundant commercial jobs vs a small residential population, tile.crime now stays 0 at both 0% and 100% tax, and growthScore again clearly differs by tax rate (20 vs 0 after 20 ticks) instead of being masked by maxed-out crime.

Current task: Remove the generic Power Plant from the build menu (replaced by the 5 specific plant types) and rebalance windmill/solar to be cheap but weak, needing several to match a coal/nuclear plant.

- [done] Removed the "Power Plant" toolbar button from index.html so it's no longer buildable in the UI. The `PRODUCER_TYPE.POWER_PLANT` / `PRODUCER_CONFIG` entry itself was left in config.js since existing tests place it directly via `grid.placeProducer(...)` as a generic utility stand-in unrelated to power-plant balance; it's harmless dead UI-only code otherwise.
- [done] Rebalanced capacities/costs: Windmill 60->40 base capacity (fluctuation 40->25), cost 3500->1500. Solar Panel peak 90->60, cost 4000->2000. Coal Plant capacity 250->300, cost 4500->4000. Nuclear Plant capacity 600->800, cost 20000->25000. Net effect: a single coal plant now needs ~5-7 windmills or ~5 solar panels (fewer, since solar is 0 at night) to match; nuclear remains the most expensive and most powerful clean option, coal the cheaper heavy polluter.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Raise Coal Power Plant cost to $12,500, and add a "Build Info" panel showing stats for the currently selected build tool.

- [done] Coal Power Plant cost raised from $4,000 to $12,500 (config.js PRODUCER_CONFIG + index.html toolbar label). Note: this supersedes the $4,000 figure from the immediately preceding rebalance task.
- [done] Added `#build-info-panel` (index.html) reusing the existing `.inspector-panel`/`.visible` show-hide CSS, plus `updateBuildInfoPanel(tool)` in main.js. It shows when any `producer_*` or `zone_*` tool is selected in the left tool drawer: cost, power output/capacity (with special-cased text for windmill fluctuation, solar day-curve, battery storage/discharge, coal pollution radius/emission), service building job/coverage tables, survey duration, or zone population/jobs-by-density and industrial pollution stats. Hidden (and mutually exclusive with the Tile Inspector) for inspect/road/bridge/bulldoze/survey.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Current task: Flip the Utility HUD header from "Capacity & Demand" to "Demand & Capacity".

- [done] Updated the `.utility-hud` header text in index.html; the meter values themselves (`updateMeter` in main.js) already rendered `demand / capacity`, so only the label was mismatched.

Validation:
- index.html diagnostics pass. No automated test covers this UI label.

Current task: Stop freshly-zoned residential tiles from starting with free population before any simulation ticks occur.

- [done] Root cause: `computeTilePopulation()` used `Math.max(1, Math.round(capacity * (0.1 + 0.9 * progress)))`, giving every residential tile a guaranteed 10%-of-capacity population (minimum 1) even at growthScore 0, i.e. immediately on placement while paused/before pressing Go.
- [done] Changed to `Math.round(capacity * Math.max(0, progress))`, so population is exactly 0 until growthScore actually rises above 0 via ticks.
- [done] Updated 3 tests in simulation.test.js that depended on the old floor: two "labor supply" tiles used `growthScore = THRESHOLD_HIGH` (the exact boundary where HIGH-tier progress is still 0) to seed population for employment-rate checks — bumped both to `GROWTH_CONFIG.MAX_SCORE` so they still provide workers under the corrected math. The labor-market-aggregates test's expected employable population at growthScore 0 was corrected from 3 to 0 (with jobsFilled/jobsAvailable/employmentRate adjusted to match).

Validation:
- All test suites pass: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.

Note: A separate, not-yet-implemented report is still open — Windmill/Solar Panel/Battery show as "disconnected" from utilities in the Tile Inspector because those producer types have no `utilityUsage` entry in PRODUCER_CONFIG (only civic services + Survey Station consume power/water/sewage today), and `formatUtil()` in main.js only special-cases that same small list of producer types. Fixing this properly means giving every producer type a utilityUsage for whichever utilities it doesn't itself produce, generalizing UtilityManager's service/survey utility-consumption pass to cover all producers, and updating the inspector's `formatUtil()` to recognize any producer with a utilityUsage instead of the hardcoded type list.

Current task: Fix Windmill/Solar Panel/Battery (and other producers) showing as disconnected in the Tile Inspector (follow-up to the note above).

- [done] Added `utilityUsage` to every producer type that lacked one, sized to consume the utilities it doesn't itself produce: all 6 power producers (Power Plant, Windmill, Solar Panel, Battery, Coal, Nuclear) now draw `{water:1, sewage:1}`; Water Pump draws `{power:2, sewage:1}`; Sewage Plant draws `{power:2, water:1}`; School/Library each `{power:1, water:1, sewage:1}`; City Hall `{power:2, water:2, sewage:2}` (Police/Fire/Hospital/Survey Station already had usage).
- [done] Merged `UtilityManager.allocateServiceUtilities` + `allocateSurveyUtilities` into one generic `allocateUtilityConsumers(grid)` that consumes utilities for ANY producer with a `utilityUsage` config (previously hardcoded to just Police/Fire/Hospital/Survey Station), so every producer's `utilityShortfall`/`operational` fields are now populated correctly. This only affects inspector display/bookkeeping — it does not gate power/water/sewage generation capacity on being "operational", to avoid an unintended new failure-cascade mechanic.
- [done] Fixed a real pre-existing bug found along the way: `RoadNetwork.computeProducerDistances`'s zero-producers-of-this-type early return used the wrong key (`roadDistances` instead of `roadDistancesMap`), which threw a TypeError as soon as any producer tried to consume a utility with no producer of that type built yet.
- [done] Generalized `formatUtil()` in main.js's Tile Inspector to check `PRODUCER_CONFIG[type].utility === key` ("Produces This Utility") and `utilityUsage[key] > 0` ("Serviced"/"Shortfall (Building Offline)"/"No Local Road") for any producer, instead of a hardcoded Survey Station/Police/Fire/Hospital list; other producers now show "Not Required" for utilities they neither produce nor consume.
- [done] Updated `tests/ore-survey.test.js`: the power plant's `usedCapacity` baseline changed from 2 to 6 (idle) and 12 to 16 (active survey), since the water tower and sewage plant now also draw baseline power from it.

Validation:
- All test suites pass: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- Manual node script confirmed: a windmill connected by road to a water tower and sewage plant now reports `operational: true` and `utilityShortfall: {power:false, water:false, sewage:false}`, while still generating its own power independently.

Current task: Mobile compatibility — add a dedicated Pan mode and touch support.

- [done] index.html: added a "Pan Map" tool button (✋) as the first General tool and made it active by default (Inspect Tile is no longer pre-selected). Added an "Auto-switch to Pan after building" checkbox (`#auto-pan-toggle`, checked by default) under the General tool group.
- [done] style.css: `.tool-btn.active`/`.tool-btn.danger.active` now get a glowing box-shadow ring in addition to the existing border/background highlight, so the active tool is unmistakable on small mobile screens. Added `.auto-pan-toggle` checkbox row styling.
- [done] main.js: default `activeTool` changed from `'inspect'` to `'pan'`. Factored tool-button activation into a new `setActiveTool(tool)` helper (updates button classes, water-highlight, inspector visibility, and the Build Info panel) so it can be reused both from toolbar clicks and auto-revert logic.
- [done] `handleCanvasClick` now treats `'pan'` exactly like `'inspect'` (tap = select/inspect tile only, never build/bulldoze/zone).
- [done] Added `this.autoSwitchToPan` (bound to the new checkbox); after any successful placement/bulldoze/survey action, if enabled, the tool automatically reverts to Pan via `setActiveTool('pan')`.
- [done] Added `touchstart`/`touchmove`/`touchend` listeners (all `{passive:false}` with `preventDefault()` to stop page scroll and suppress duplicate synthetic mouse events): single-finger drag ALWAYS pans the camera regardless of active tool (never drag-builds), and a tap that moved less than 8px triggers `handleCanvasClick` with a synthetic `{clientX, clientY}` object once released.

Validation:
- All test suites pass unchanged (no test touches main.js): `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- main.js/index.html/style.css diagnostics pass. Not verified on an actual mobile device/emulator in this session — recommend a manual touch-device smoke test (pan-drag, tap-to-inspect in Pan mode, tap-to-build in a placement tool, auto-revert after building).

Current task: Fix mobile layout where the header, tool drawer, utility HUD, and inspector panels all overlapped the map (no responsive CSS existed at all before this).

- [done] Added a `@media (max-width: 820px)` block in style.css: header wraps to a second row and its stats-counter becomes a horizontally scrollable strip (seed control hidden to save space); the overlay-mode picker becomes horizontally scrollable instead of overflowing.
- [done] `.tool-drawer` (left) and `.utility-hud` (right) become full-height off-canvas drawers on mobile (`transform: translateX(...)`, hidden by default) instead of always-visible floating panels, so the map underneath is fully clear by default.
- [done] Added two floating round toggle buttons (🛠️ tools / 📊 stats) inside the viewport (index.html), wired in main.js to toggle a `.mobile-open` class on each drawer; opening one auto-closes the other. Selecting a tool (`setActiveTool`) auto-closes the tool drawer afterward so the map is immediately visible to tap-build.
- [done] `.inspector-panel`/`#build-info-panel` (Tile Inspector / Build Info) get mobile-only positioning: full width minus margins, and raised off the bottom edge (`bottom: 76px`) so they don't collide with the new toggle buttons.

Validation:
- All test suites pass unchanged (no test touches CSS/HTML/main.js UI wiring): `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- main.js/index.html/style.css diagnostics pass. Still not verified on a real device/emulator — recommend checking a phone-width viewport (e.g. browser devtools device toolbar at ~390px) to confirm the drawers open/close cleanly and nothing overlaps.

Current task: Real-device mobile feedback fixes (Samsung phone with on-screen nav bar covering the toggle buttons; auto-switch-to-Pan resetting road placement).

- [done] Raised the 🛠️/📊 mobile toggle buttons from `bottom: 16px` to `bottom: calc(90px + env(safe-area-inset-bottom, 0px))` and added `viewport-fit=cover` to the HTML viewport meta tag so `env(safe-area-inset-bottom)` actually resolves on devices that report it, clearing the phone's on-screen nav/gesture bar. Adjusted `.tool-drawer`/`.utility-hud` bottom offset and `.inspector-panel`/`#build-info-panel` bottom offset (76px -> 150px) to match, so nothing overlaps the raised buttons.
- [done] Auto-switch-to-Pan now only fires for single-placement buildings (`activeTool.startsWith('producer_')`) after a successful build, not for repeatable tools like road/bridge/zone/bulldoze/survey — so placing a run of roads no longer kicks you back to Pan/Inspect after every tile. Updated the checkbox label to "Auto-switch to Pan after placing a building" to reflect the narrower scope.

Validation:
- All test suites pass unchanged: `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- main.js/index.html/style.css diagnostics pass. Still not verified on the reporting user's actual device — the safe-area-inset offset is a best-effort fix since Android's on-screen nav bar isn't always reported via that CSS env var consistently across browsers; may need further tuning based on next feedback.

Current task: Require Windmill/Solar Panel to be adjacent (including diagonals) to a Battery Storage building to transmit power.

- [done] Grid.js: added `getNeighbors8(x, y)` (8-directional including diagonals), leaving the existing orthogonal `getNeighbors(x, y)` untouched for road/utility network logic.
- [done] UtilityManager.updatePowerGeneration: Windmill/Solar Panel producers now check `hasAdjacentBattery(grid, x, y)` (via `getNeighbors8`) each tick. If no Battery Storage is adjacent, `producer.capacity = 0` and `producer.hasBatteryConnection = false`; if adjacent, capacity is computed as before (wind fluctuation / solar day-curve) and `hasBatteryConnection = true`.
- [done] Tile Inspector (main.js `updateInspector`): Windmill/Solar Panel tiles show `⚠️ Offline (Must be adjacent to Battery Storage)` in place of the load/capacity readout when disconnected; otherwise shows normal output as before.
- [done] Build Info panel (main.js `updateBuildInfoPanel`): Windmill and Solar Panel entries now include a "Requires: Adjacent (incl. diagonals) to Battery Storage to transmit power" row.

Validation:
- All test suites pass unchanged (no test places Windmill/Solar Panel producers): `npm test`, test:ore, test:river, test:services, test:tax, test:roads, test:income, test:industrial, test:tax-revenue, test:school, test:service-expenses.
- Manual node script confirmed: a windmill diagonally adjacent to a battery generates power normally (`hasBatteryConnection: true`, nonzero capacity); a solar panel with no adjacent battery is gated to `capacity: 0` (`hasBatteryConnection: false`).

Current task: Fix correctness bugs and architectural issues identified in project review (Part 1).

- [done] Stop ResourceManager from overwriting civic service staffing (`updateProducerJobs` limited to resource/factory categories).
- [done] Fix placement economic tick: placement no longer adds income or advances time, runs preview tick(false) only; simTick() is the only income/expense path.
- [done] Fix pause state leaking into resources: gate ResourceManager.update and prepareTick on advanceWorld.
- [done] Make preview tick(false) non-mutating: power allocation in preview mode skips wind rerolls and battery charge/discharge writes.
- [done] Clear sticky famine populationLoss when food shortfall is 0.
- [done] Align wind/solar HUD capacity and battery charging: unified `contributesPowerToGrid` connectivity check.
- [done] Make agricultural tiles first-class: implement occupancy from filled jobs, utility consumption, fire ignition/spread, crime inclusion, and zone counters.
- [done] Define TERRAIN_GENERATION_CONFIG.DEFAULT_RANDOM_SEED (12345) and implement Overworld Biome generation (Plains, Hilly, Mountainous, Swamp).
- [done] Implement consumer goods consumption each advancing tick; commercial tax doubles when supplied with consumer goods; goods serve as happiness bonus.
- [done] Add Oil Refinery producer and fuel economy: oil derrick produces ~100 oil/tick, refinery refines 100 oil into 50 fuel/tick; fuel demand across residential/commercial/industrial/agricultural; fuel shortfall halves taxes.
- [done] Expose Police, Fire, and Hospital overlay buttons in toolbar.
- [done] Version sync: created src/version.js (0.1.6), synced package.json, automated version test.
- [done] Removed unused BASE_INCOME table and retargeted industrial economics tests.
- [done] Renamed seed storage key to bc2000_map_seed with legacy migration.
- [done] Created test manager `tests/run-all.js` running all 21 test suites via `npm test`.
- [done] Implement realistic fire model: unoccupied zones and roads do not burn; mountains, forests, and inhabited zones burn; extinguished fires repair gradually and produce 0 tax until repaired; boosted fire suppression power to 55.
- [done] Removed MONEY_MULTIPLIER and hardcoded 10x pre-scaled values; kept road maintenance at $1.
- [done] Chunked 64x64 terrain cache in Renderer.js (max 2048px canvases).

Validation:
- `node tests/run-all.js` passes all 21 test suites.

Current task: Complete remaining review items (Part 2).

- [done] Add arms and tanks stockpile chips to HUD and main.js updater.
- [done] Fix desktop Pan tool left-drag camera panning and prevent non-repeatable tools from drag-painting.
- [done] Update Grid.generateProceduralTerrain to accept direct `biome` parameter.
- [done] Add refinery and oil derrick build info details to `updateBuildInfoPanel`.
- [done] Update `docs/DESIGN.md` with all agreed user specifications and architectural rules (rev 5).

Validation:
- `npm test` runs `tests/run-all.js` and all 21 test suites pass with 0 failures.
- Desktop left-drag with Pan tool pans camera; click without drag inspects tile.
- Drag-painting restricted to repeatable tools (roads, bridges, tunnels, zones, bulldoze).
- Grid.generateProceduralTerrain accepts biome directly and scales terrain correctly.
- Arms and Tanks chips display in top HUD.
- docs/DESIGN.md updated to rev 5.

- [done] Move Oil Derrick and Silo buttons from Utility Producers to Production group in index.html.

- [done] Add Clinic civic service ($2,000, 5,000 pop cap, does not grow larger, patient capacity, rendering, build info, and unit tests).
- [done] Added water/sewage utility demand to Windmill and Solar Panel so all power producers report connected or shortfall status in the Tile Inspector.
- [done] Moved Civic Services ahead of Production in the tool drawer so the Clinic is visible earlier in the normal build-tool flow.
- [done] Updated the synchronized application version to 0.1.6.

Validation:
- `npm test` passes all 22 test files.
- `node tests/wind-solar-grid.test.js` and `node tests/version-sync.test.js` pass.
- Source, HTML, stylesheet, and test diagnostics pass.

Current task: Synchronize project documentation after architecture review.

- [done] Updated `docs/DESIGN.md` to describe the current implementation instead of resolved historical defects.
- [done] Separated implemented Phase 1 work, outstanding Phase 1 work, and the later hybrid/world-map roadmap.
- [done] Documented this file as the AI-agent start/end handoff log so interrupted work can resume safely.
- [done] Bumped the synchronized application version to 0.1.8 for the standalone save feature.
- [done] Made mountain terrain immune to fire so long mountain surveys cannot be canceled by mountain ignition.

Validation:
- `npm test` passes all 22 test files.
- Documentation references were checked against current source, package scripts, and task status.

Current task: Rework emergency services coverage + survey overlay & cost.

Emergency Services Coverage:
- [done] Add COVERAGE_CONFIG to config.js (base direct range 5, road range 10, road-adjacent range 3, +1 per filled job).
- [done] Create CoverageManager.js with cached coverage Sets for fire, police, medical (hospital+clinic shared).
- [done] Implement 3-step coverage algorithm: direct Manhattan range → connected-road BFS → road-side range.
- [done] Add coverage invalidation on road, bridge, tunnel, and producer changes; staffing changes are detected by the cache state signature.
- [done] Refactor ServiceManager.js to use CoverageManager for fire, police, and shared medical coverage, while keeping RoadNetwork for school/library/cityHall.
- [done] Update fire suppression and service overlay state to use cached emergency coverage.

Survey Overlay:
- [done] Add survey overlay button to index.html.
- [done] Gate renderSurveyStatus behind overlayMode === 'survey' in Renderer.js.
- [done] Add survey overlay rendering in renderOverlay (show ore type/color for discovered tiles, progress for active surveys).

Survey Cost & Cancellation:
- [done] Add SURVEY_COST_PER_TICK: 10 to config.
- [done] Add cancelSurvey helper to Grid.js; cancellation clears station and target progress.
- [done] Update Simulation.updateSurveys to charge $10 before progress, cancel if treasury is insufficient, target/station is on fire, or station is non-operational.
- [done] Update main.js simTick to pass treasury and deduct surveyExpenses.

Verification:
- [done] `node tests/coverage.test.js` passes coverage geometry, cache reuse, staffing scale, and shared medical coverage.
- [done] `node tests/service-buildings.test.js` passes cached service integration and fire suppression checks.
- [done] `npm test` passes all 23 test files.
- [ ] Manual test coverage overlay, survey overlay, survey cost/cancellation.

Current task: Add standalone JSON game save export/import.

- [done] Capture all tile and producer fields, including terrain, fire state/damage/repair, pollution, crime, utilities, surveys, population, jobs, and resource state.
- [done] Require the game to be paused before saving and always restore imported games paused.

Validation:
- `node tests/save-game.test.js` passes round-trip, malformed JSON, unsupported version, and running-save rejection checks.
- `npm test` passes all 24 test files.
- Browser download/file-picker smoke testing remains manual.

Current task: Optimize mobile simulation ticks with active tile registries and pollution gating.

- [done] Add Grid-maintained active registries for zoned tiles, roads, fire candidates, active fires, and repairing tiles; rebuild them after map generation and save import.
- [done] Replace full-map simulation, utility, service, road-network, resource, crime, fire, and relocation work with active-tile iteration where only modified gameplay tiles matter.
- [done] Gate pollution recomputation behind a state signature covering terrain, industrial density/fire state, sewage shortfalls, and relevant producer throughput/capacity/operational state.
- [done] Preserve emergency coverage on non-zoned tiles while clearing only prior cached coverage when coverage shrinks.
- [done] Add `tests/active-tile-registry.test.js` for registry maintenance and stable/dirty pollution behavior.
- [done] Bump the patch version to 0.1.9 for the mobile simulation performance fix.

Validation:
- `npm test` passes all 25 test files.
- Core simulation, resource, tax, fire, service, coverage, river pollution, and save-import regression tests pass.
- Manual mobile smoke test remains required: run a minimal city for several minutes at 1x/2x/5x and confirm the browser remains responsive.

Current task: Prevent runaway wilderness fires from freezing mobile browsers.

- [done] Root cause: forest fires spread to every adjacent forest at the same 15% rate as urban fires, creating thousands of active fires and 5 hospital injuries per wilderness tile.
- [done] Add a dedicated 0.5% forest-to-forest spread rate while retaining the existing 15% urban/structure spread rate.
- [done] Count fire injuries only for burning inhabited zones and producer tiles, not uninhabited forests.
- [done] Add a regression proving uninhabited forest fires create no hospital demand.
- [done] Bump the patch version to 0.1.10.

Validation:
- `node tests/fire-repair.test.js` passes.
- `npm test` passes all 25 test files.

Current task: Correct wilderness fire injury and spread balance.

- [done] Set forest ignition to 0.001% per tile per tick and forest-to-forest spread to 1% per adjacent tile per tick.
- [done] Give each wildfire one 0.1% chance to cause one injury for its whole burn duration, rather than rolling a new injury chance each tick; inhabited zones and producer tiles retain their existing injury behavior.
- [done] Confirm burnt-out forest tiles become non-flammable and cannot spread fire further.
- [done] Bump the patch version to 0.1.12.

Current task: Make Windmills and Solar Panels passive battery-fed generators.

- [done] Remove direct water and sewage requirements from Windmills and Solar Panels.
- [done] Require only an adjacent Battery Storage whose tile is road-connected; neither the renewable nor battery utility status gates renewable generation.
- [done] Use hybrid renewable dispatch: live wind/solar output serves demand first, only surplus charges its adjacent battery, stored energy discharges only during a renewable deficit, then dispatchable plants cover any remaining demand.
- [done] Update the Tile Inspector to show renewables as not requiring utilities.
- [done] Add focused tests for renewable-first supply, surplus charging, and battery-only deficit discharge.
- [done] Bump the patch version to 0.1.14.

Validation:
- `node tests/wind-solar-grid.test.js` passes.

Current task: Add a deterministic small-town stability soak test.

- [done] Build a 30x30 town with a 20% tax rate, 13 residential, 8 commercial, 15 agricultural, and 3 industrial zones.
- [done] Place the Water Pump upstream of the Sewage Plant on a directed river, plus a road-connected battery and adjacent windmill.
- [done] Run 50 controlled ticks and assert finite stats, bounded population/patient demand, no uncontrolled fires, and an uncontaminated upstream Water Pump.

Validation:
- `node tests/small-town-soak.test.js` passes.

Current task: Fix passive renewable status in the Tile Inspector.

- [done] Initialize Windmill and Solar Panel battery-adjacency state at placement so the inspector is correct before the first tick.
- [done] Add a DOM-free producer connection-status helper used by the Tile Inspector.
- [done] Show `Connected via Battery Storage` for a renewable next to a road-connected battery and clear error text when the connection exists.
- [done] Add `tests/inspector-status.test.js` covering connected and missing-battery inspector status.
- [done] Bump the patch version to 0.1.15.

Validation:
- `npm test` passes all 27 test files.

Current task: Reduce header clutter and correct passive renewable warnings.

- [done] Suppress the obsolete `Needs Utilities!` producer-header warning for passive Windmills and Solar Panels.
- [done] Add tabbed right-HUD views: Utility Demand & Capacity, Storage, and Service Budgets.
- [done] Move all stockpile values from the top header to the Storage tab without changing the existing HUD update IDs.
- [done] Bump the patch version to 0.1.16.

Validation:
- Inspector status and wind/solar regression tests pass.
- `npm test` passes all 27 test files.

Current task: Profile and eliminate remaining mobile tick stalls.

Goal: A 200x200 city must remain responsive on mobile. Work in the stages below, mark each stage `[done]` only after its stated check passes, and stop after any failing check to repair that stage before continuing.

Stage 1 - Establish measurements:
- [ ] Add optional per-stage timing to `Simulation.tick()` behind a debug flag or explicit test helper. Measure prepare, stats-before, utilities, pollution, services, crime, fire, resources, stats-after, and growth separately.
- [ ] Extend `tests/small-town-soak.test.js` with a 200x200 minimal-town fixture matching the user layout: 20% tax; upstream Water Pump; downstream Sewage Plant; battery + adjacent Windmill; roads; R/C/I/A zones. Run 50 ticks with controlled randomness and assert finite/bounded state only. Do not add machine-specific timing thresholds yet.
- [ ] Run the soak fixture locally and record the slowest stage(s) in this file before optimizing them.

Stage 2 - Stop repeated service work:
- [ ] Make `ServiceManager.updateServices()` skip coverage/assignment work unless service buildings, road topology, staffing, operational state, or service budgets changed. Keep staffing/running-cost updates correct when population or workforce changes.
- [ ] Replace transient coordinate-string handling in service coverage with tile references or numeric tile IDs where it is a measured bottleneck.
- [ ] Check: service, coverage, clinic, and 200x200 soak tests pass.

Stage 3 - Track pollution deltas:
- [ ] Replace full-grid pollution reset/aggregate work with a tracked set of polluted/water-polluted tiles. Clear and recompute only prior affected tiles plus current source radii when `pollutionDirty` is true.
- [ ] Keep river pollution, contaminated Water Pump behavior, forest absorption, and pollution HUD totals equivalent to current behavior.
- [ ] Check: river-pollution, simulation pollution checks, and 200x200 soak test pass.

Stage 4 - Reduce stable-tick work:
- [ ] Cache or incrementally maintain pollution average/max and road cost so `computeStats()` does not need full-grid scans twice every tick on a stable map.
- [ ] Preserve exact stats after placement, bulldoze, fire destruction/repair, density changes, and save import.
- [ ] Check: small-town soak and all accounting/tax/resource tests pass.

Stage 5 - Browser rendering/UI budget:
- [ ] Measure render-loop and HUD/inspector update time separately. Update HUD/inspector only when displayed values change or at a bounded mobile cadence; never delay simulation state.
- [ ] Confirm terrain chunks and overlays do not force whole-map redraws after normal simulation ticks.
- [ ] Check manually on the reporting phone at 1x, 2x, and 5x for at least 50 ticks.

Stage 6 - Release verification:
- [ ] Run `npm test`, editor diagnostics, and `git diff --check`.
- [ ] Update `docs/DESIGN.md` with the measured bottleneck and final caching rules.
- [ ] Bump the patch version only after the mobile reproduction is verified on-device.
